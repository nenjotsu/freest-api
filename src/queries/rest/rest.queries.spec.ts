import { Test, TestingModule } from '@nestjs/testing';
import { RestServiceQueries } from './rest.queries';

describe('RestServiceQueries', () => {
  // let service: RestServiceQueries;

  // beforeAll(async () => {
  //   const moduleName: TestingModule = await Test.createTestingModule({
  //     providers: [RestServiceQueries],
  //   }).compile();
  //   service = moduleName.get<RestServiceQueries>(RestServiceQueries);
  // });
  // it('should be defined', () => {
  //   expect(service).toBeDefined();
  // });
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
