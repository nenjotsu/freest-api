import { Test, TestingModule } from '@nestjs/testing';
import { SuperheroServiceQueries } from './superhero.queries';

describe('SuperheroServiceQueries', () => {
  // let service: SuperheroServiceQueries;

  // beforeAll(async () => {
  //   const moduleName: TestingModule = await Test.createTestingModule({
  //     providers: [SuperheroServiceQueries],
  //   }).compile();
  //   service = moduleName.get<SuperheroServiceQueries>(SuperheroServiceQueries);
  // });
  // it('should be defined', () => {
  //   expect(service).toBeDefined();
  // });
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
